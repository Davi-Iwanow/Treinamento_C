#include <stdio.h>
#include <string.h>

//strlen = tamanho da string
//strcopy = copia uma string
//strcat = concatena/junta duas strings
//strcmp = compara duas strings //s1 seja .. s2(retorna 0 se =, retorno menor q 0 se menor e maior q 0 se maior )

